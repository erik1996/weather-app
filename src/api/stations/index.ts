import { addStations } from "./add-stations";
import { getStations } from "./get-stations";
import { removeStations } from "./remove-stations";

export const stations = {
  getStations,
  addStations,
  removeStations,
};
